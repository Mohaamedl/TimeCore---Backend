package com.odin.domain;

/**
 * Enum representing the type of verification.
 */
public enum VerificationType {
    /**
     * Verification via mobile phone.
     */
    MOBILE,
    /**
     * Verification via email.
     */
    EMAIL
}
